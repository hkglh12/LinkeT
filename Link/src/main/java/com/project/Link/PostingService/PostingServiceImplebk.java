package com.project.Link.PostingService;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.project.Link.Posting.Dao.PostingDao;

@Service
/*public class PostingServiceImple implements PostingService {*/
public class PostingServiceImplebk{
	/*
	 * @Autowired private PostingDao pDao;
	 * 
	 * public PostingServiceImple(){}
	 * 
	 * 
	 * public PostingDao getpDao() { return pDao; }
	 * 
	 * 
	 * public void setpDao(PostingDao pDao) { this.pDao = pDao; }
	 * 
	 * 
	 * @Override public int getCount(String targetBoard, String prefix,int serial) {
	 * int result = pDao.get(targetBoard, prefix, serial); return result; }
	 * 
	 * @Override public int updateCount(String targetBoard,String prefix, int count,
	 * int serial) { int result = pDao.update(targetBoard,prefix,count, serial);
	 * return result; }
	 */
}
